# Calculator-with-History
🧮 Calculator with History  A Python project that performs multiple calculations (add, subtract, multiply, divide) from user input. Supports comma‑separated expressions, saves results into a history file, and provides options to view or clear history. Built with pure Python and file handling.
